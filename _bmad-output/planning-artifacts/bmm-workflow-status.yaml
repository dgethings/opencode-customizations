# Workflow Status Template

# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2026-01-11T22:14:36+0400"
project: opencode-customizations
project_type: enterprise-bmad-method
selected_track: enterprise
field_type: brownfield
workflow_path: "/Users/dgethings/git/opencode-customizations/_bmad/bmm/workflows/workflow-status/paths/enterprise-brownfield.yaml"
workflow_status:
  phase_0_documentation:
    name: "Documentation"
    condition: if_undocumented
    workflows:
      - id: document-project
        required: true
        agent: analyst
        command: /bmad:bmm:workflows:document-project
        status: required
  phase_1_analysis:
    name: "Analysis (Optional)"
    optional: true
    workflows:
      - id: brainstorm-project
        agent: analyst
        command: /bmad:bmm:workflows:brainstorming
        status: required
      - id: research
        agent: analyst
        command: /bmad:bmm:workflows:research
        status: required
  phase_2_planning:
    name: "Planning"
    required: true
    workflows:
      - id: prd
        required: true
        agent: pm
        command: /bmad:bmm:workflows:create-prd
        status: required
      - id: create-ux-design
        condition: if_has_ui
        agent: ux-designer
        command: /bmad:bmm:workflows:create-ux-design
        status: conditional
  phase_3_solutioning:
    name: "Solutioning"
    required: true
    workflows:
      - id: create-architecture
        required: true
        agent: architect
        command: /bmad:bmm:workflows:create-architecture
        status: required
      - id: create-epics-and-stories
        required: true
        agent: pm
        command: /bmad:bmm:workflows:create-epics-and-stories
        status: required
      - id: test-design
        optional: true
        agent: tea
        command: /bmad:bmm:workflows:test-design
        status: optional
      - id: implementation-readiness
        required: true
        agent: architect
        command: /bmad:bmm:workflows:implementation-readiness
        status: required
  phase_4_implementation:
    name: "Implementation"
    required: true
    workflows:
      - id: sprint-planning
        required: true
        agent: sm
        command: /bmad:bmm:workflows:sprint-planning
        status: required
      - id: beads-integration
        optional: true
        agent: sm
        command: /bmad:bmm:workflows:beads-integration
        status: optional
continuing_existing: true
brainstorm_requested: true
research_requested: true
